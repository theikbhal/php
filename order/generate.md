# LLM Generation Prompt for Custom Extensions

Use the following prompt if you want an AI assistant to modify or extend this app:

> You are updating a zero-dependency single-file PHP 8.x web app (`index.php`) utilizing SQLite3, Tailwind CSS CDN, and vanilla JS. Maintain dark mode styling (`#09090b`), keep all logic strictly in one file, preserve the 10-item cap constraint for ordered tasks, and ensure no additional npm or composer packages are introduced.
